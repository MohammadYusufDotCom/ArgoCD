apiVersion: v1
kind: Service
metadata:
  name: {{ include "argocd_helm.fullname" . }}
  labels:
    {{- include "argocd_helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "argocd_helm.selectorLabels" . | nindent 4 }}
